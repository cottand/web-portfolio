{
    http_port 80
    auto_https off
}
:80 {
	bind 0.0.0.0

    handle /* {
        root * /srv
        file_server
        try_files {path} /index.html
        encode gzip zstd
        tracing {
            span web-portfolio
        }
    }
}
